<%- include("partials/header") -%>
<h1>Home</h1>
<p>
  <%= homeDetails %> </p>
<% posts.forEach(function(post){ %>
<h1><%= post.title %></h1>
<p>
  <%= post.content.substring(0,50)+"..." %>
   <a href="/posts/<%= post.title %>">Read more</a>
</p>
<% }) %>
<a href="/compose"><button type="submit" class="btn btn-primary"> Compose </button></a>

<%- include("partials/footer") -%>

<!-- substring(0,50) = limit the characters -->
